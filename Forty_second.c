# include<stdio.h>

//Practising loop control statements

int main () {
    
                //Here the 'i' is called the iterator or controller.
    
    //We can also write i++ instead i = i + 1. This is called post - increment operator.

    //i++ means - use and increase

    //But ++i means - increase and then use. This is called pre - increment operator.

    int i = 1;
    /* printf("%d \n", i++);
    printf("%d \n", i); */

    printf("%d \n", ++i);
    printf("%d \n", i);

    return 0;
}