# include<stdio.h>
int main() {
    //Change the variable names
    int x, y;
    
    printf("The value of x is");
    scanf("%d", &x);

    printf("The value of y is");
    scanf("%d", &y);

    printf("The answer is %d", x + y);

    return 0;
}
