# include<stdio.h>
//Finding the area of a square when the length of its side is an integer
int main() {
    //To do : Change variable names to meaningful names
    
    int a;
    
    printf("Enter the length of the side");
    scanf("%d", &a);
    printf("The area of the Square is %d", a * a);

    return 0;
}